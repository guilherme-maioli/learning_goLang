package main

import "fmt"

func main() {
	fmt.Println("Por que usariamos um campo anônimo incluido em vez de utilizar um campo nomeado? ")

	fmt.Println("Pois o campo anônimo nos da a propriedade da struct `ser` daquele tipo também e não `ter` o tipo.")

	fmt.Println("Com isso, a struct nova também tem acesso aos metodos do campo anônimo.")

}
