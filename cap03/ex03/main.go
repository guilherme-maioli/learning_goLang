package main

import "fmt"

func main() {
	fmt.Print("O que é escopo? Como determinamos o escopo de uma variável em Go? ")

	fmt.Println("É o que define como uma váriavel, constante pode ser acessada.")
	fmt.Println("Definimos um escopo com base nos {}. Tudo que estiver dentro destes, está contido em um escopo específico.")
}
